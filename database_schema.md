# DPMPTSP Database Schema

This document represents the database schema found in the `dpmptsp` project.

## Table: `users`
| Column Name | Data Type | Constraints | Description |
|-------------|-----------|-------------|-------------|
| `id` | int | PRIMARY KEY, AUTO_INCREMENT | Unique identifier for user |
| `username` | varchar(100) | NOT NULL, UNIQUE | User login name |
| `email` | varchar(255) | NOT NULL, UNIQUE | User email address |
| `full_name` | varchar(100) | DEFAULT NULL | - |
| `phone_number` | varchar(20) | DEFAULT NULL | - |
| `nik` | varchar(20) | UNIQUE, DEFAULT NULL | - |
| `password` | varchar(255) | NOT NULL | Hashed password |
| `role` | enum('warga','dpmptsp','dinas_kesehatan','penanaman_modal') | NOT NULL, DEFAULT 'warga' | User access role |
| `dinas` | varchar(50) | DEFAULT NULL | Department association |
| `created_at` | timestamp | NOT NULL, DEFAULT CURRENT_TIMESTAMP | - |
| `signature_file` | varchar(255) | DEFAULT NULL | Path/filename of the signature file |

## Table: `documents`
| Column Name | Data Type | Constraints | Description |
|-------------|-----------|-------------|-------------|
| `id` | int | PRIMARY KEY, AUTO_INCREMENT | Unique identifier for the document |
| `user_id` | int | NOT NULL, FOREIGN KEY (users.id) | Submitter user ID |
| `doc_name` | varchar(255) | NOT NULL | Document name/title |
| `dinas` | varchar(50) | DEFAULT NULL | Target department |
| `data_json` | longtext | DEFAULT NULL | JSON structured data including form & files |
| `uploaded_at` | timestamp | NOT NULL, DEFAULT CURRENT_TIMESTAMP | - |
| `forwarded_at` | timestamp | DEFAULT NULL | - |
| `forwarded_by` | int | FOREIGN KEY (users.id) | ID of the user who forwarded the document |
| `status` | varchar(64) | NOT NULL | Current document processing status |
| `rejection_reason` | text | DEFAULT NULL | Reason for rejection |
| `verifier_id` | int | FOREIGN KEY (users.id) | ID of the user verifying |
| `verified_at` | timestamp | DEFAULT NULL | Verification status timestamp |
| `response_filename` | varchar(255)| DEFAULT NULL | - |
| `response_uploaded_at`| timestamp | DEFAULT NULL | - |
| `sent_to_citizen_at` | timestamp | DEFAULT NULL | - |
| `rekomendasi_filename`| varchar(255)| DEFAULT NULL | - |
| `rekomendasi_uploaded_at`| datetime | DEFAULT NULL | - |
| `izin_filename` | varchar(255)| DEFAULT NULL | - |
| `izin_uploaded_at` | datetime | DEFAULT NULL | - |

## Table: `investments`
| Column Name | Data Type | Constraints | Description |
|-------------|-----------|-------------|-------------|
| `id` | int | PRIMARY KEY, AUTO_INCREMENT | Unique identifier for the investment record|
| `amount` | decimal(15,2) | NOT NULL | Investment amount |
| `month` | int | NOT NULL, UNIQUE KEY with `year` | Month of investment |
| `year` | int | NOT NULL, UNIQUE KEY with `month` | Year of investment |
| `created_at` | timestamp | DEFAULT CURRENT_TIMESTAMP | - |
| `updated_at` | timestamp | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - |

---
*Generated by Antigravity*
